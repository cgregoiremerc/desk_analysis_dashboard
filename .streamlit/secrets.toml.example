# Template for .streamlit/secrets.toml — copy this to .streamlit/secrets.toml
# (which is gitignored) and fill in the real value. On Streamlit Community
# Cloud, paste the real value into the app's Secrets settings instead.

veslink_token = "YOUR_VESLINK_API_TOKEN_HERE"
